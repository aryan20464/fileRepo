apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaBridge
metadata:
  name: my-bridge-test
  namespace: rforex-kafka
spec:
  replicas: 1
  bootstrapServers: my-cluster-kafka-bootstrap:9093
  authentication:
    type: scram-sha-512
    username: bridge-test-user
    passwordSecret:
      secretName: bridge-test-user
      password: password
  http:
    port: 8080
    consumer:
      enabled: true
    producer:
      enabled: true
